import React, { useEffect, useState } from "react";
import { Toast } from "../../components/Toast";


export function ToastContainer(listToasts) {
  return(
    console.log(listToasts)
    //listToasts.map(t => <div>{t}</div>)
  )
}
